---node
const layout = 'layouts/country.webc';

function permalink(data) {
	const lowerCase = data.country.country.toLowerCase();
	return `/country/${lowerCase}/index.html`;
}

const pagination = {
  size: 1,
  data: "countries",
  alias: "country",
  resolve: "values",
  reverse: true
}
---
