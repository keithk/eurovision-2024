---node
const layout = 'layouts/country.webc';

function permalink(data) {
	return `/country/${data.country.countryCode}/index.html`;
}

const pagination = {
  size: 1,
  data: "countries",
  alias: "country",
  resolve: "values",
  reverse: true
}
---
